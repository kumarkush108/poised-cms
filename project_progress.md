# Project Progress

_Last rebuilt from a full codebase audit on 2026-06-12._

## Current project status

This is a Laravel 12 corporate website for Poised Technology with a public marketing site and an early-stage admin panel skeleton. The public site is functionally complete (static content). The admin panel exists visually but has no working backend logic — no real authentication, no content management, no message handling.

## Completed modules

- **Public marketing site**: home, about, services, solutions, contact pages — all built out with full content, carousels, brand sections, testimonials, and forms (UI only).
- **Public layout system**: `layouts/app.blade.php` + partials (`head`, `topbar`, `navbar`, `footer`, `scripts`, `brand`) — reusable across all public pages.
- **Admin layout shell**: `admin/layouts/app.blade.php` + partials (`sidebar`, `navbar`, `footer`) — Bootstrap 5 based, with sidebar navigation and profile dropdown.
- **Admin login screen**: `admin/auth/login.blade.php` with full form markup, validation error display, "remember me" checkbox, and theme toggle.
- **Admin forgot-password screen**: `admin/auth/forgot-password.blade.php` view exists with a route that validates an email field.
- **Admin dashboard shell**: `admin/dashboard/index.blade.php` with 4 metric cards (currently hardcoded values).
- **Base Laravel scaffolding**: default `User` model, default migrations (users/sessions/password resets, cache, jobs/queues), `UserFactory`, `DatabaseSeeder` (seeds one test user, `test@example.com`).
- **Frontend build pipeline**: Vite 6 + Tailwind CSS 4 configured and working (`resources/css/app.css`, `resources/js/app.js`).

## In-progress modules

CMS transformation (Phases A-J per approved roadmap). Phase A complete. Phase B (CMS Core Database Schema) complete. Phase C (Media Library) complete. Phase D (Theme Settings) complete. Phase E (Schema Alignment) complete. Phase E.1 (Final Schema Gap Closure) complete. Phase F (Page & Section CRUD) complete, pending review. Phase I (Frontend CMS Integration) complete. Production Hardening Sprint 1 complete. Phase H (Contact Messages) complete.

## Pending modules

- **Real forgot-password flow**: token generation, reset email (mail is currently `log` driver), and a reset-password form/route.
- **Media Library (Phase C)**: upload UI, storage handling, and admin list/edit/delete are complete (see Change Log).
- **Menu management (deferred CRUD)**: `menus`/`menu_items` schema and seed data exist; no admin CRUD yet.
- **Theme settings UI**: complete (see Change Log, Phase D). Public-facing consumption is now wired up (Phase I).
- **Dashboard real metrics**: "Messages" count is now real (Phase H). Pages/Services/Solutions counts (12/8/6) remain hardcoded — not in Phase H scope.
- **API routes**: `routes/api.php` does not exist; no API layer is configured.
- **New page creation, section add/remove, drag-and-drop ordering**: explicitly out of scope for Phase F (see Phase F Change Log); deferred to a later phase if needed.

## Known bugs/issues

- Forgot-password submission always returns a generic success message regardless of whether the email exists.

## Technical debt

- No services/jobs/events/listeners/policies — any future business logic will need this scaffolding built up from nothing.
- Test suite only contains the default Laravel example tests (HTTP 200 check on `/`); no coverage of admin auth, forms, or any future CMS logic.
- Public contact/appointment forms render in Blade but have no server-side handling — form submissions currently go nowhere.

## Recent discoveries

- Confirmed via full codebase read that `CLAUDE.md`, `project_progress.md`, and `notes.md` did not previously exist on disk — this audit is the first time these docs are being generated from the actual implementation rather than assumptions.
- `database/database.sqlite` exists locally (~86KB) and is the active dev database; it contains only the default Laravel tables plus the seeded test user.
- Confirmed no git repository is initialized in this working directory.

## Recommended next priorities

1. Decide on and create the CMS data model (migrations + models) for `contact_messages` before building out that admin sidebar section (Phase H).
2. Wire the public contact/appointment forms to a real controller that persists submissions (depends on `contact_messages` table, Phase H).
3. Replace hardcoded dashboard metrics with real counts once the above models exist.
4. Expand test coverage to cover admin auth flows (now in place) and any new CMS controllers as they're built.
5. Address the Class B/C hardcoded blocks noted in Phase E.1/Phase I (home's second EV/tech block, home's about-image collage + experience badge, about's EV Section cards, `richtext` sanitization) if/when a future phase wants them CMS-driven.
6. Note (commit hygiene): the two most recent commits prior to Phase E were both labeled "Phase C: Media Library" but their diffs actually cover Phase C+D and Phase F respectively — no functional issue, but future commit messages should match their actual phase content.

## Change Log

### 2026-06-12 — Phase A: Admin Authentication & Security

- **What changed**: Implemented real Laravel-native authentication for the admin panel. `Admin\AuthController::loginSubmit` now uses `Auth::attempt()` with session regeneration; added `logout()` with session invalidation + token regeneration. Protected `/admin/dashboard` with the `auth` middleware and moved login/forgot-password routes under `guest` middleware. Added `admin.logout` route (POST) and login throttling (`throttle:5,1`). Wired the navbar "Logout" link to a real POST form. Configured `redirectGuestsTo`/`redirectUsersTo` in `bootstrap/app.php` to point at `admin.login`/`admin.dashboard` respectively (since the app has no default `login`-named route).
- **Files modified**: `routes/web.php`, `app/Http/Controllers/Admin/AuthController.php`, `bootstrap/app.php`, `resources/views/admin/partials/navbar.blade.php`.
- **Why**: Phase A of the approved CMS roadmap — the admin area was previously fully open and login accepted any credentials.
- **Verified**: Manual end-to-end test via `php artisan serve` — unauthenticated `/admin/dashboard` redirects to `/admin/login` (302); valid login (`test@example.com`/`password`) redirects to dashboard (200); authenticated visit to `/admin/login` redirects to dashboard; logout invalidates session and dashboard becomes inaccessible again; invalid credentials redirect back to login with validation error.

### 2026-06-12 — Phase A follow-up: scope global auth redirects to /admin

- **What changed**: `redirectGuestsTo`/`redirectUsersTo` in `bootstrap/app.php` are global Laravel settings affecting any `auth`/`guest`-protected route, not just admin ones. Updated both closures to check `$request->is('admin/*')` — admin paths still redirect to `admin.login`/`admin.dashboard`, any other `auth`/`guest`-protected route (none exist yet) falls back to `home`. Prevents a future public-facing auth flow from being redirected into the admin panel.
- **Files modified**: `bootstrap/app.php`.
- **Why**: Reviewer flagged that the original global redirects could unintentionally affect future frontend authentication.
- **Verified**: Re-ran the full Phase A manual test suite — all admin login/logout/redirect behaviors unchanged.

### 2026-06-12 — Phase B: CMS Core Database Schema

- **What changed**: Implemented the field-based CMS schema approved after design review. Created 10 new tables: `media`, `pages`, `page_sections`, `section_fields`, `section_items`, `item_fields`, `menus`, `menu_items`, `settings`, `cache_versions`. `page_sections`/`section_items` are purely structural (page/section/order/active); all content lives in `section_fields`/`item_fields` as `field_key` → `value`/`media_id` rows, with the field set per `section_key`/`item_type` declared in `config/cms/templates.php` (the Template Registry, exposed via `App\Cms\TemplateRegistry`). Added 10 Eloquent models with relationships. Registered the registry config via `AppServiceProvider::register()` (`mergeConfigFrom`). Seeded: 11 `theme`-group `settings` rows (colors + logo/favicon); the 5 existing public pages (`home`, `about`, `services`, `solutions`, `contact`) as `is_system=true` rows with their `page_sections` populated per their template's `allowed_sections` (no field content yet — Blade views unchanged); `header`/`footer` `menus` each with 5 `menu_items` linking to the 5 pages. `Page` model guards (`static::deleting`/`static::updating`) prevent deleting `is_system` pages and prevent changing `slug`/`template` on `is_system` pages or after creation on any page.
- **Files created**: 10 migrations (`database/migrations/2026_06_12_1000{00..09}_*.php`), 10 models (`app/Models/{Media,Page,PageSection,SectionField,SectionItem,ItemField,Menu,MenuItem,Setting,CacheVersion}.php`), `config/cms/templates.php`, `app/Cms/TemplateRegistry.php`, `database/seeders/{SettingsSeeder,PagesSeeder,MenusSeeder}.php`.
- **Files modified**: `app/Providers/AppServiceProvider.php` (merge `config/cms/templates.php`), `database/seeders/DatabaseSeeder.php` (call new seeders).
- **Database changes**: 10 new tables (see above); existing tables untouched.
- **Routes**: none added/changed.
- **Packages**: none added.
- **Why**: Phase B of the approved CMS roadmap — establishes a schema that supports new section/item types via Template Registry config entries alone, with no future migrations.
- **Verified**: `php artisan migrate` ran clean (all 10 new tables created); seeders produced 11 settings rows, 5 pages with correct templates and section counts (home=8, about=3, services=4, solutions=5, contact=3), 2 menus with 5 items each; tinker test confirmed `is_system` page delete and slug/template change both throw `RuntimeException`; `php artisan serve` smoke test confirmed `/`, `/about`, `/admin/login` all return 200 — no regression to existing routes/views.
- **Rollback**: `php artisan migrate:rollback --step=10` (drops the 10 new tables in reverse order); delete the 10 migration files, 10 model files, `config/cms/templates.php`, `app/Cms/TemplateRegistry.php`, 3 seeder files; revert `AppServiceProvider.php` and `DatabaseSeeder.php`.

### 2026-06-12 — Phase B hardening: close `forceDelete()` bypass on system pages

- **What changed**: A post-review audit found that `App\Models\Page`'s `is_system` protection guards (`deleting`/`updating`) did not cover Laravel's separate `forceDeleting` lifecycle event, so `->forceDelete()` could hard-delete an `is_system` page without triggering any guard. Added a `static::forceDeleting()` listener in `Page::boot()` that throws `\RuntimeException('System pages cannot be force-deleted.')` for `is_system` pages, mirroring the existing `deleting` guard. Also enabled the in-memory SQLite testing database (`phpunit.xml`, previously commented out) so the test suite no longer points at the real MySQL dev database (`poised_cms`), and added `tests/Unit/PageSystemProtectionTest.php` covering all `Page` model protection rules: system-page delete/force-delete/slug-change/template-change all throw; non-system page slug/template changes after creation throw; non-system page soft-delete, force-delete, and other-field updates succeed normally.
- **Files modified**: `app/Models/Page.php`, `phpunit.xml`.
- **Files created**: `tests/Unit/PageSystemProtectionTest.php`.
- **Database changes**: none.
- **Routes**: none added/changed.
- **Packages**: none added.
- **Why**: closes a protection gap identified during the Phase B architecture review before formal close-out, per explicit request not to defer it to Phase F.
- **Verified**: `php artisan test` — 11 passed (18 assertions), including all 9 new `PageSystemProtectionTest` cases and the 2 pre-existing example tests.
- **Rollback**: revert `app/Models/Page.php` (remove the `forceDeleting` listener), revert `phpunit.xml` (re-comment the `DB_CONNECTION`/`DB_DATABASE` testing env lines), delete `tests/Unit/PageSystemProtectionTest.php`.

### 2026-06-12 — Phase C: Media Library

- **What changed**: Built the admin Media Library on top of the Phase B `media` table/model (no schema changes). Added `App\Http\Controllers\Admin\MediaController` with `index` (paginated grid), `store` (validated upload to the `public` disk, creates a `Media` row), `update` (edit `alt_text`/`title`), and `destroy` (soft delete only — `$media->delete()`). Added 4 new routes under the existing `auth` middleware group: `admin.media.index` (GET `/admin/media`), `admin.media.store` (POST), `admin.media.update` (PATCH `/admin/media/{media}`), `admin.media.destroy` (DELETE). Added `resources/views/admin/media/index.blade.php` (upload form + media grid with inline edit/delete, extends `admin.layouts.app`). Added a "Media Library" link to `admin/partials/sidebar.blade.php` (first previously-dead sidebar slot to become functional). Ran `php artisan storage:link` (did not exist before) to make the `public` disk browser-accessible. **Lifecycle hardening**: added `Media::boot()` with a `forceDeleting` listener that deletes the physical file via `Storage::disk($media->disk)->delete($media->path)` — soft delete (`delete()`) remains DB-only and keeps the file on disk for restorability; only a future force-delete (no UI yet) triggers file removal.
- **Files created**: `app/Http/Controllers/Admin/MediaController.php`, `resources/views/admin/media/index.blade.php`, `tests/Feature/Admin/MediaLibraryTest.php`.
- **Files modified**: `app/Models/Media.php` (added `boot()`/`forceDeleting` file-cleanup listener), `routes/web.php` (4 new routes), `resources/views/admin/partials/sidebar.blade.php` (new "Media Library" link).
- **Database changes**: none — reused the existing `media` table/model from Phase B.
- **Routes**: added `admin.media.index`, `admin.media.store`, `admin.media.update`, `admin.media.destroy` (all under `auth` middleware, prefix `admin`). No existing routes changed.
- **Environment**: created the `public/storage` → `storage/app/public` symlink via `php artisan storage:link` (did not exist previously).
- **Packages**: none added.
- **Why**: Phase C of the approved CMS roadmap — provides upload/storage/management for media referenced (via `media_id`) by pages, sections, items, and settings in future phases.
- **Verified**: `php artisan test` — 18 passed (39 assertions), including the new 7-test `MediaLibraryTest` (guest redirect, successful upload + disk assertion, rejected invalid mime type, rejected oversized file, metadata update, soft-delete keeps file on disk, force-delete removes file from disk) and all pre-existing tests (Phase B hardening's `PageSystemProtectionTest` + example tests) still pass. `php artisan route:list --name=admin.media` confirms all 4 routes registered with no name collisions.
- **Rollback**: delete `app/Http/Controllers/Admin/MediaController.php`, `resources/views/admin/media/index.blade.php`, `tests/Feature/Admin/MediaLibraryTest.php`; revert `app/Models/Media.php` (remove `boot()`/`forceDeleting` listener and the `Storage` import); revert `routes/web.php` (remove the 4 `admin.media.*` routes and the `MediaController` import); revert `resources/views/admin/partials/sidebar.blade.php` (remove the "Media Library" link); optionally remove the `public/storage` symlink (`Remove-Item public/storage` — uploaded files remain safe in `storage/app/public`, only the symlink is removed). No migration to roll back (no schema changes).

### 2026-06-12 — Phase D: Theme Settings Management

- **What changed**: Built the admin Theme Settings screen on top of the Phase B `settings` table (no schema changes) and the existing `Setting::media()` relation. Added `App\Http\Controllers\Admin\SettingController` with `index` (loads the 11 `theme`-group `settings` rows keyed by `key`, plus all image-mime `Media` records for the logo/favicon dropdowns) and `update` (validates and persists the 9 color values and the 2 media references). Added 2 new routes under the existing `auth` middleware group: `admin.settings.index` (GET `/admin/settings`), `admin.settings.update` (PATCH). Added `resources/views/admin/settings/index.blade.php` — a single static form (color pickers for the 9 theme colors, plain `<select>` dropdowns of existing image media for logo/favicon, with a server-rendered preview thumbnail of the currently-selected logo/favicon). Added a "Theme Settings" link to `admin/partials/sidebar.blade.php` (second previously-dead sidebar slot to become functional, placed between "Media Library" and "Home CMS").
- **Scope decisions (per architecture review)**: this phase deliberately ships the *smallest* production-safe implementation — edit-only against the 11 fixed seeded rows (no creation of new settings), no reusable media picker, no new JS files, no modal upload/select workflow. Logo/favicon selection is a plain dropdown populated from `Media::where('mime_type', 'like', 'image/%')`. A reusable media picker remains deferred to Phase F, where it will have multiple real consumers (`section_fields.media_id`, `item_fields.media_id`).
- **Files created**: `app/Http/Controllers/Admin/SettingController.php`, `resources/views/admin/settings/index.blade.php`, `tests/Feature/Admin/SettingsTest.php`.
- **Files modified**: `routes/web.php` (2 new routes + `SettingController` import), `resources/views/admin/partials/sidebar.blade.php` (new "Theme Settings" link). `app/Models/Setting.php` already had a `media()` `belongsTo(Media::class)` relation from Phase B — no model change was needed.
- **Database changes**: none — reused the existing `settings` table/rows from Phase B (`SettingsSeeder`).
- **Routes**: added `admin.settings.index` (GET), `admin.settings.update` (PATCH), both under `auth` middleware, prefix `admin`. No existing routes changed.
- **Validation**: each of the 9 color settings (`primary_color`, `secondary_color`, `accent_color`, `header_background`, `footer_background`, `button_color`, `button_hover_color`, `text_color`, `link_color`) requires `regex:/^#[0-9A-Fa-f]{6}$/`. `logo_media_id`/`favicon_media_id` are `nullable` and validated with `Rule::exists('media', 'id')->where(mime_type like 'image/%')` — rejects non-existent media ids and non-image media (e.g. PDFs) for both fields.
- **Packages**: none added.
- **Why**: Phase D of the approved CMS roadmap (revised scope per architecture review) — provides the first real consumer of `media_id` (via `Setting::media()`) ahead of Phase F, using the smallest viable UI.
- **Verified**: `php artisan test` — 25 passed (54 assertions), including the new 7-test `SettingsTest` (guest redirect, view settings, update colors, reject invalid hex, set logo to existing image media, reject non-existent media id, reject non-image media for favicon) and all pre-existing tests (Phase B hardening, Phase C Media Library, example tests) still pass. `php artisan route:list --name=admin.settings` confirms both routes registered with no name collisions.
- **Rollback**: delete `app/Http/Controllers/Admin/SettingController.php`, `resources/views/admin/settings/index.blade.php`, `tests/Feature/Admin/SettingsTest.php`; revert `routes/web.php` (remove the 2 `admin.settings.*` routes and the `SettingController` import); revert `resources/views/admin/partials/sidebar.blade.php` (remove the "Theme Settings" link). No migration to roll back, no model changes to revert (`Setting::media()` predates this phase).

### 2026-06-14 — Phase F: Page & Section CRUD via TemplateRegistry

- **What changed**: Built the admin "Pages" module on top of the Phase B field-based schema (`pages`, `page_sections`, `section_fields`, `section_items`, `item_fields`) and `App\Cms\TemplateRegistry`, per the approved Phase F architecture review (APPROVE WITH CHANGES). Scope covers the 5 existing system pages only:
  - `Admin\PageController` — `index` (lists the 5 pages with template label/status/section count), `edit` (loads a page with its sections, section fields, items, and item fields), `update` (edits page metadata: title, meta_title/description/keywords, canonical_url, robots, og_title/description/og_image_id, status, published_at). `slug` and `template` are displayed read-only and excluded from the update payload — `Page`'s existing `updating` guards remain untouched and untriggered.
  - `Admin\PageSectionController` — `update` validates and `updateOrCreate`s `section_fields` rows (keyed by `page_section_id` + `field_key`) from `TemplateRegistry::sectionFields($section->section_key)`, plus toggles `page_sections.is_active`. Unknown/unregistered `section_key`s degrade gracefully: `sectionFields()`/`itemSchema()` return `[]`/`null`, so the form renders a "not defined in the Template Registry" notice and only `is_active` is updated.
  - `Admin\SectionItemController` — `store` (add a new `section_items` row + `item_fields`, `item_type` and field schema from `TemplateRegistry::itemSchema()`, `order_column` = current max + 1), `update` (edit `item_fields` + `is_active` via `updateOrCreate`), `destroy` (soft delete via `SectionItem::delete()`), `move` (swap `order_column` with the previous/next sibling for up/down reordering — no drag-and-drop).
  - `App\Http\Controllers\Admin\Concerns\HandlesTemplateFields` (new trait, shared by `PageSectionController`/`SectionItemController`) — builds per-field validation rules from a `TemplateRegistry` field schema (`string`→`max:255`, `text`/`richtext`→`string`, `url`→`url|max:255`, `integer`→`integer`, `media`→`exists:media,id`, honoring each field's `required` flag), normalizes empty-string media `<select>` values to `null` before validation (otherwise `exists` fails on `""` even with `nullable`), and persists validated `fields.*` input via `updateOrCreate(['field_key' => ...], [...])` onto a `PageSection`/`SectionItem`'s `fields()` relation.
  - Views: `admin/pages/index.blade.php` (page list), `admin/pages/edit.blade.php` (page-meta form + one form per section for its fields/active toggle + per-item edit/move/delete forms + an "add item" form per section with an item schema), `admin/pages/partials/field-input.blade.php` (renders one field as text/textarea/number/url/media-select based on `TemplateRegistry` type), `resources/views/admin/partials/media-select.blade.php` (new shared, generic `<select>` of all `Media` rows — reused for `og_image_id` and any `media`-type section/item field; Phase D's settings dropdown was left as-is).
  - Sidebar: replaced the 4 dead "Home/About/Solutions/Services CMS" links in `admin/partials/sidebar.blade.php` with a single "Pages" link to `admin.pages.index`. "Contact Messages" remains a dead link (Phase H).
- **Files created**: `app/Http/Controllers/Admin/PageController.php`, `app/Http/Controllers/Admin/PageSectionController.php`, `app/Http/Controllers/Admin/SectionItemController.php`, `app/Http/Controllers/Admin/Concerns/HandlesTemplateFields.php`, `resources/views/admin/pages/index.blade.php`, `resources/views/admin/pages/edit.blade.php`, `resources/views/admin/pages/partials/field-input.blade.php`, `resources/views/admin/partials/media-select.blade.php`, `tests/Feature/Admin/PageManagementTest.php`.
- **Files modified**: `routes/web.php` (8 new routes + 3 new controller imports), `resources/views/admin/partials/sidebar.blade.php` (replaced 4 dead links with "Pages").
- **Database changes**: none — reused the Phase B `pages`/`page_sections`/`section_fields`/`section_items`/`item_fields` tables and models as-is.
- **Routes** (all under `auth` middleware, prefix `admin`): `admin.pages.index` (GET `/admin/pages`), `admin.pages.edit` (GET `/admin/pages/{page}`), `admin.pages.update` (PATCH), `admin.page-sections.update` (PATCH `/admin/page-sections/{section}`), `admin.section-items.store` (POST `/admin/page-sections/{section}/items`), `admin.section-items.update` (PATCH `/admin/section-items/{item}`), `admin.section-items.destroy` (DELETE), `admin.section-items.move` (POST `/admin/section-items/{item}/move`).
- **Validation**: page metadata uses inline `$request->validate([...])` (status restricted to `draft`/`published`; `canonical_url`/`og_image_id` etc. follow existing conventions). Section/item fields are validated dynamically per `TemplateRegistry` field type/required flag via `HandlesTemplateFields::templateFieldRules()` — e.g. `hero.heading` and `cta.heading` are `required`, `ev_solutions`/`testimonials`/etc. item fields enforce their own `required` flags (e.g. `solution-card.title`, `testimonial.author`/`quote`).
- **Packages**: none added.
- **Why**: Phase F of the approved CMS roadmap, implemented per the prior architecture review's "APPROVE WITH CHANGES" recommendation — gives admins real content-editing for the 5 system pages while keeping the smallest production-safe scope (no new-page creation, no section add/remove, no drag-and-drop, no frontend rendering changes).
- **Verified**: `php artisan test` — 40 passed (107 assertions), including the new 15-test `PageManagementTest` (guest redirect; pages index; page edit view; page metadata update incl. status validation and OG image assignment; section field update incl. required-field validation, media field assignment, and rejection of an unknown media id; graceful handling of an unregistered `section_key`; full item add/update/remove lifecycle; item `required`-field validation; item move-up reordering including the first-item no-op case) and all 25 pre-existing tests (Phase B hardening, Phase C Media Library, Phase D Settings, examples) still pass.
- **Rollback**: delete `app/Http/Controllers/Admin/{Page,PageSection,SectionItem}Controller.php`, `app/Http/Controllers/Admin/Concerns/HandlesTemplateFields.php`, `resources/views/admin/pages/`, `resources/views/admin/partials/media-select.blade.php`, `tests/Feature/Admin/PageManagementTest.php`; revert `routes/web.php` (remove the 8 `admin.pages.*`/`admin.page-sections.*`/`admin.section-items.*` routes and the 3 new controller imports); revert `resources/views/admin/partials/sidebar.blade.php` (restore the 4 dead CMS links in place of "Pages"). No migration to roll back, no model changes to revert.

### 2026-06-14 — Phase E: Schema Alignment

- **What changed**: Closed the schema gaps identified by the Phase I Preflight Audit between `config/cms/templates.php` and the actual public Blade markup, so every major content block on the 5 system pages now has a CMS representation, ahead of Phase I (frontend rewiring). Config-only + seeder changes — no migrations, no Blade/public-rendering changes, no admin controller/view changes (Phase F's generic TemplateRegistry-driven admin UI renders all of the below automatically).
  - **`hero` section** (additive, backward compatible): added optional `autoplay`/`interval` integer fields, and a new repeatable `items` schema (`item_type: hero-slide`, fields: `heading` (required), `subheading`, `body`, `button_text`, `button_url`, `background_image`). Existing single-hero fields (`heading`, `subheading`, `button_text`, `button_url`, `background_image`) are unchanged — non-carousel pages (about/services/solutions/contact) keep using them; the home page's 3-slide carousel is now representable via `hero-slide` items.
  - **New `stats` section** (`item_type: stat`, fields: `label`, `value` — both string to allow values like "100+", "99%", "24/7", "PAN India"). Added to `home`, `standard_page` (about), and `service_page` (services) `allowed_sections`.
  - **New `faq` section** (`item_type: faq-item`, fields: `question`, `answer`, both required). Added to `contact_page`.
  - **New `process_steps` section** (`item_type: process-step`, fields: `step_number` (string, e.g. "01"), `title`, `description`). Added to `landing_page` (solutions).
  - **New `checklist` section** (`item_type: checklist-item`, fields: `text` (required), `description`, `icon`) — the maintainable item-based replacement for "checklist of features" blocks (About's "Innovation Driven/EV Ecosystem", Services' "Enterprise Solutions/Cloud Infrastructure/EV Technology/Automation Systems", Solutions' "Scalable Architecture/Cloud Native/EV Charging/Enterprise Security"). Added to `standard_page`, `service_page`, `landing_page`. `richtext` was deliberately not used for this content.
  - **`contact_info` section**: added optional `phone_secondary`/`email_secondary` string fields (Contact page shows two phone numbers and two emails).
  - **Template `allowed_sections` realigned**:
    - `home`: added `stats` → `['hero', 'ev_solutions', 'stats', 'brand_logos', 'about', 'features', 'services_grid', 'appointment', 'testimonials']`.
    - `standard_page` (about): `['hero', 'content', 'checklist', 'stats', 'features', 'cta']` (was `['hero', 'content', 'cta']`).
    - `service_page` (services): `['hero', 'content', 'checklist', 'services_grid', 'features', 'stats', 'cta']` (was `['hero', 'services_grid', 'features', 'cta']`; added `content` for the intro block).
    - `landing_page` (solutions): `['hero', 'content', 'checklist', 'services_grid', 'process_steps', 'cta']` (was `['hero', 'features', 'brand_logos', 'testimonials', 'cta']` — these did not match the actual solutions page content at all and were removed).
    - `contact_page`: `['hero', 'contact_info', 'faq', 'cta']` (was `['hero', 'contact_info', 'content']`; `content` replaced with `faq`/`cta` since the page has no generic text block beyond hero/cards/form/map/faq).
  - **`PagesSeeder` updated**: in addition to the existing per-page `page_sections` rows (now reflecting the realigned `allowed_sections`), the seeder now also creates structural `section_items` rows (item_type/order_column/is_active only, no field content — matching the Phase B "structural first, content later" pattern) for every repeatable block, sized to match the actual markup: home (hero=3, ev_solutions=3, stats=4, brand_logos=3, features=4, services_grid=8, testimonials=2), about (checklist=2, stats=4, features=3), services (checklist=4, services_grid=6, features=4, stats=4), solutions (checklist=4, services_grid=6, process_steps=4), contact (faq=3). Seeding is idempotent via `firstOrCreate` keyed on `order_column`.
- **Files modified**: `config/cms/templates.php`, `database/seeders/PagesSeeder.php`, `tests/Feature/Admin/PageManagementTest.php` (2 pre-existing section-item tests updated — see below).
- **Files created**: `tests/Unit/TemplateRegistrySchemaTest.php` (8 tests — new section/item schema definitions, realigned `allowed_sections`, and confirms original `hero`/`ev_solutions`/`services_grid`/`testimonials`/`brand_logos`/`features` definitions are unchanged), `tests/Feature/PagesSeederSchemaTest.php` (6 tests — verifies seeded section order and item counts per page, plus seeder idempotency).
- **Existing test updates (backward compatibility)**: `PageManagementTest::test_authenticated_user_can_add_update_and_remove_section_item` and `::test_section_item_move_up_swaps_order_with_previous_sibling`/`::test_section_item_move_up_is_noop_for_first_item` previously assumed the home page's `ev_solutions` section started with zero items; since Phase E's seeder now pre-creates 3 structural `solution-card` items there, these tests were updated to account for (rather than assume the absence of) pre-seeded items — the underlying add/update/remove/reorder behavior is unchanged.
- **Database changes**: none — reused the existing `page_sections`/`section_items`/`section_fields`/`item_fields` tables. No new migrations.
- **Routes**: none added/changed.
- **Packages**: none added.
- **Why**: Phase I (frontend CMS integration) was blocked by schema mismatches found in the Phase I Preflight Audit (hero carousel, stat counters, FAQ, process steps, checklists, dual phone/email, and `landing_page`'s `allowed_sections` not matching the solutions page at all). This phase closes those gaps via config + seeder only, per the Project Phase Status Audit's recommendation that Phase E run before Phase I.
- **Verified**: `php artisan test` — 54 passed (180 assertions): all 40 pre-existing tests (with the 2 updated `PageManagementTest` cases) plus the 14 new Phase E tests.
- **Remaining blockers before Phase I** (not addressed by this phase, by design — schema-only scope):
  - The home page's `about` section (image collage + 3 inline counters) and the second "Next-Generation Technology & EV Solutions" block (progress bars + video modal) have no dedicated schema representation — these remain hardcoded/visual-only until a future phase defines a schema for them.
  - `richtext` (`content.body`) still has no sanitization (pre-existing, flagged in Phase F).
  - Phase I itself: `partials/sections/*` still render hardcoded markup and must be rewired to read from the now-aligned schema.
- **Rollback**: revert `config/cms/templates.php` and `database/seeders/PagesSeeder.php` to their pre-Phase-E versions; revert the 2 updated assertions in `tests/Feature/Admin/PageManagementTest.php`; delete `tests/Unit/TemplateRegistrySchemaTest.php` and `tests/Feature/PagesSeederSchemaTest.php`. No migration to roll back.

### 2026-06-14 — Phase E.1: Final Schema Gap Closure

- **What changed**: A follow-up validation review of Phase E identified 4 remaining Class-A schema gaps (business-editable content with no CMS representation at all). This phase closes all 4, config + seeder only, no migrations, no Blade/frontend changes:
  - **Home "about" counters** (3 about-fact counters: Awards Winning/Complete Cases/Happy Clients, each a "9999"-style value): gave the existing `about` section an `items` schema reusing the `stat` item type (`item_type: stat`, fields `label`/`value` — identical shape to the `stats` section's items). Each `page_sections` row has its own independent `section_items`, so reusing the `stat` item_type for `about`'s items does not collide with home's separate `stats` section. `PagesSeeder::ITEM_COUNTS['home']['about'] = 3`.
  - **About page "Vision & Mission"**: added a new reusable `cards` section (`item_type: info-card`, fields `icon`, `title` (required), `description`) — a generic icon+title+description card list, deliberately page-agnostic. Added `cards` to `standard_page.allowed_sections` (for About's Vision/Mission, seeded with 2 items) and to `contact_page.allowed_sections` (for Contact's Working Hours/Quick Support, seeded with 2 items) — one new section type serving two different pages/content blocks, per the "avoid single-page-only solution" requirement.
  - **Services/Solutions card highlights**: added an optional `highlights` field (type `text`, newline-delimited bullet list) to the existing `service-card` item schema (`services_grid` section). Renders as a `<textarea>` via the existing `field-input.blade.php` `text`/`richtext` case — no admin UI changes needed. Preserves the per-card bullet sub-lists (e.g. "AC/DC Chargers / Smart Monitoring / Energy Optimization") that previously had no field to live in.
  - **Contact intro content**: restored `content` to `contact_page.allowed_sections` (heading + richtext body, for the "Let's Build Something Amazing Together" intro copy) and added `cards` (see above) for the "Working Hours"/"Quick Support" info blocks, seeded with 2 `info-card` items.
  - **Template `allowed_sections` updated**:
    - `standard_page` (about): `['hero', 'content', 'checklist', 'cards', 'stats', 'features', 'cta']` (was `[..., 'checklist', 'stats', 'features', 'cta']` — inserted `cards`).
    - `contact_page`: `['hero', 'contact_info', 'content', 'cards', 'faq', 'cta']` (was `['hero', 'contact_info', 'faq', 'cta']` — re-added `content`, added `cards`).
  - **`PagesSeeder::ITEM_COUNTS` updated**: `home.about = 3` (stat items), `about.cards = 2` (info-card items for Vision/Mission), `contact.cards = 2` (info-card items for Working Hours/Quick Support).
- **Files modified**: `config/cms/templates.php` (about section items, new `cards` section, `service-card.highlights` field, `standard_page`/`contact_page` allowed_sections), `database/seeders/PagesSeeder.php` (`ITEM_COUNTS` additions), `tests/Unit/TemplateRegistrySchemaTest.php` (updated `allowed_sections` assertions + 4 new test methods), `tests/Feature/PagesSeederSchemaTest.php` (updated home/about/contact section/item assertions).
- **Files created**: none.
- **Database changes**: none — reused existing `page_sections`/`section_fields`/`section_items`/`item_fields` tables. No new migrations.
- **Routes**: none added/changed.
- **Packages**: none added.
- **Backward compatibility**: all additions are additive (new optional fields, new section type, new allowed_sections entries) — no existing field, item type, or `allowed_sections` entry was removed or renamed. All pre-existing Phase A–F and Phase E tests continue to pass unmodified except the 3 `allowed_sections`/section-order assertions in `TemplateRegistrySchemaTest`/`PagesSeederSchemaTest` that were expected to change (about/contact now have 1-2 additional sections).
- **Why**: the Phase E Validation Review found that 4 content blocks (Home about-counters, About Vision/Mission, Services/Solutions per-card bullet highlights, Contact intro/working-hours/quick-support copy) had no schema destination, risking either permanent hardcoding or content loss during Phase I migration. Per the review's explicit instruction, Phase I must not begin until these are resolved.
- **Verified**: `php artisan test` — 58 passed (211 assertions), up from 54 passed (180 assertions). All pre-existing tests pass; 4 new schema tests added (`test_about_section_supports_repeatable_stats`, `test_cards_section_and_info_card_item_schema_exist`, `test_service_card_supports_highlights`, `test_contact_page_allows_content_for_intro_copy`).
- **Remaining known gaps (explicitly Class B/C, accepted as out of scope)**:
  - Home's second "Next-Generation Technology & EV Solutions" block (progress bars + video modal) — no schema; remains hardcoded (Class C, large effort, doesn't block other sections' migration).
  - Home's "about" image collage (3 images vs. single `image` field) and "25/Years Experience" badge overlay — cosmetic/structural (Class B/C).
  - About's "EV Section" 2 small icon/title cards and Home's `ev_solutions` dual-heading overlap — minor (Class C).
  - `richtext` sanitization still unaddressed (pre-existing, Phase F).
- **Rollback**: revert `config/cms/templates.php` and `database/seeders/PagesSeeder.php` to their pre-Phase-E.1 versions; revert the 4 assertion/test changes in `tests/Unit/TemplateRegistrySchemaTest.php` and `tests/Feature/PagesSeederSchemaTest.php`. No migration to roll back.

### 2026-06-15 — Phase I: Frontend CMS Integration

- **What changed**: Converted all 5 public pages (home, about, services, solutions, contact) and the shared public layout from hardcoded Blade content to CMS-driven rendering, using the schema finalized in Phases E/E.1. Visual structure, CSS, and JS dependencies are unchanged — only content sources changed.
  - **Page resolution**: added `App\Http\Controllers\PageController` (public, namespace `App\Http\Controllers`) with `show(string $slug, string $view)` — loads the `Page` by slug with active `sections`/`items`/`fields`/`media` eager-loaded, 404s if the page is missing or not `published`, and passes `page` plus a `sections` collection keyed by `section_key` to the view. All 5 routes in `routes/web.php` now call `app(PageController::class)->show($slug, $view)` instead of returning a static view directly.
  - **Shared rendering helper**: added `App\Cms\Content` (`app/Cms/Content.php`) with `field()`, `itemField()`, `items()`, `mediaUrl()`, `lines()`, `settingValue()`, `settingMediaUrl()` — a small static helper used throughout the converted Blade views to read `section_fields`/`item_fields` values with fallback to the original hardcoded copy, avoiding duplicated null-checking/loading logic across 5 pages.
  - **`Content::items()` fallback rule**: returns a section's real `section_items` only if at least one active item has at least one populated `item_fields` row (`$items->contains(fn ($item) => $item->fields->isNotEmpty())`); otherwise falls back to the page's hardcoded default item array. This is necessary because `PagesSeeder` (Phase E/E.1) pre-creates *structural* `section_items` rows (for admin UI scaffolding) with zero field content — without this rule, every carousel/grid/stat/card section on a freshly-seeded install would render blank instead of the original copy.
  - **Theme settings integration**: `AppServiceProvider::boot()` changed from `View::share('themeSettings', ...)` (computed once at app boot) to `View::composer('*', fn ($view) => $view->with('themeSettings', $this->loadThemeSettings()))` — re-resolves the `theme`-group `settings` (keyed by `key`, with `media` eager-loaded) on every view render, so settings changes take effect immediately for both production (persistent workers) and tests (settings seeded mid-test). The public layout/partials read `primary_color`/`secondary_color`/`accent_color`/`success_color`/`warning_color`/`danger_color`/`dark_color`/`light_color` via `Content::settingValue()` into a `<style id="cms-theme-overrides">` block of CSS custom-property overrides, and `logo`/`favicon` via `Content::settingMediaUrl()` with fallback to the existing static asset paths.
  - **Page conversions** (all 5 pages, each wrapping the existing markup in `@php use App\Cms\Content; ... @endphp` blocks per section, with `Content::field()`/`Content::items()`/`Content::itemField()` replacing hardcoded strings — no markup/CSS/JS structure changed):
    - **home**: hero carousel (3 `hero-slide` items), `ev_solutions`, `stats` (4 counters), `brand_logos`, `about` (incl. 3 `stat`-typed counters per Phase E.1), `features`, `services_grid` (8 cards incl. `highlights`), `appointment` heading/body, `testimonials` (2 items).
    - **about**: hero, `content` (heading + richtext body), `checklist` (2 items), `cards` (Vision/Mission, 2 `info-card` items), `stats` (4), `features`, `cta`.
    - **services**: hero, `content`, `checklist` (4 items), `services_grid` (6 cards incl. `highlights`), `features`, `stats` (4), `cta`.
    - **solutions**: hero, `content` (intro heading/body), `checklist` (4 differentiators), `services_grid` (6 solution cards incl. `highlights`), `process_steps` (4 steps), `cta`.
    - **contact**: hero, `contact_info` (address/phone/phone_secondary/email/email_secondary/map_embed_url across 3 cards + map iframe), `content` + `cards` (Working Hours/Quick Support, 2 `info-card` items), `faq` (3 items, rendered as a dynamic accordion), `cta`.
  - **Media integration**: `Content::mediaUrl()` resolves a `media`-type field's `Media` model to `->url`, falling back to the existing static `asset(...)` path when no media is set — used for hero `background_image` fields across all 5 pages, plus the navbar/footer logo and the page favicon via `Content::settingMediaUrl()`.
  - **Bugfix (latent, from prior Phase F-aliasing edit)**: `routes/web.php`'s `admin.pages.index`/`admin.pages.edit`/`admin.pages.update` routes were still referencing `PageController::class` (now resolving to the new *public* `PageController`, which has no `index`/`edit`/`update` methods) instead of `AdminPageController::class` (the Phase F admin controller, aliased but not substituted in these 3 routes). Fixed by pointing all 3 routes at `AdminPageController::class` — this was causing 500 errors on every admin Pages-management route.
  - **`tests/Feature/ExampleTest.php` fix**: enabled `RefreshDatabase` and ran `SettingsSeeder`/`PagesSeeder` in the test, since `/` now queries the `pages`/`settings` tables via the new `PageController` (the stock test previously had `RefreshDatabase` commented out and would fail with "no such table: pages").
- **Files created**: `app/Http/Controllers/PageController.php`, `app/Cms/Content.php`, `resources/views/partials/theme-style.blade.php` (renders the `<style id="cms-theme-overrides">` CSS custom-property block from `themeSettings`), `tests/Feature/PublicPagesTest.php` (11 tests).
- **Files modified**: `routes/web.php` (5 public routes now use `PageController::show()`; fixed 3 misrouted `admin.pages.*` routes to `AdminPageController`), `app/Providers/AppServiceProvider.php` (`View::share` → `View::composer('*', ...)` for `themeSettings`), `app/Models/Media.php` (added `getUrlAttribute()` accessor backed by `Storage::disk($this->disk)->url($this->path)`), `database/seeders/SettingsSeeder.php` (added `success_color`/`warning_color`/`danger_color`/`dark_color`/`light_color` to the seeded `theme` settings, completing the 10-color set required by Phase I), `resources/views/partials/{head,navbar,footer}.blade.php` (include `theme-style` partial, resolve logo/favicon via `Content::settingMediaUrl()` with static-asset fallback), `resources/views/pages/{home,about,services,solutions,contact}.blade.php` (full CMS-driven conversion as described above), `tests/Feature/ExampleTest.php`.
- **Database changes**: none — purely consumes the existing `pages`/`page_sections`/`section_fields`/`section_items`/`item_fields`/`settings`/`media` tables from Phases B/C/D/E/E.1. No new migrations.
- **Routes**: no new route names added. 5 public route closures replaced with `PageController::show()` calls (same names: `home`, `about`, `solutions`, `services`, `contact`). 3 `admin.pages.*` routes repointed to the correct controller (bugfix, not a new route).
- **Packages**: none added.
- **Why**: Phase I of the approved CMS roadmap — makes the public site genuinely CMS-driven (content editable via the Phase F admin UI flows straight to the public pages) while preserving the existing visual design, per the binding requirement that "only content sources should change."
- **Fallback behavior summary**: every piece of CMS content has a hardcoded default matching the pre-Phase-I markup. A field/section/item renders its default when: the page/section is missing, the section is `is_active = false`, a field has no `section_fields`/`item_fields` row (or an empty value), or — for repeatable items — no item in the section has any populated field row (handles Phase E/E.1's pre-seeded empty structural items). This guarantees the site never renders blank/broken content on a freshly-migrated or partially-edited install.
- **Verified**: `php artisan test` — 69 passed (248 assertions), up from 58 passed (211 assertions) before this phase. New `PublicPagesTest` (11 tests) covers: all 5 pages resolve and render default content; an unpublished page 404s; a `section_fields` override replaces default content; an `item_fields` override replaces a default item's content; a section with all-empty items falls back to default items; an inactive section falls back to default content (override field is ignored); a `background_image` media field renders its URL on the page; theme color settings render as CSS overrides and update live when changed; logo/favicon media settings render their URLs; and an empty `settings` table doesn't break rendering (falls back to defaults). All pre-existing tests (Phases A–F, E.1) continue to pass unmodified except `ExampleTest` (updated, see above).
- **Remaining work after Phase I (Class B/C, explicitly out of scope, unchanged from Phase E.1)**:
  - Home's second "Next-Generation Technology & EV Solutions" block (progress bars + video modal) — still fully hardcoded, no schema.
  - Home's "about" image collage (3 images vs. single `image` field) and "25/Years Experience" badge overlay — cosmetic/structural, still hardcoded.
  - About's "EV Section" 2 small icon/title cards and Home's `ev_solutions` dual-heading overlap — still hardcoded.
  - `richtext` (`content.body`) fields render via `{!! !!}` with no HTML sanitization on input or output (pre-existing gap, unchanged).
  - `brand_logos` items' `url` field (link target) is defined in the schema but not rendered/used on the frontend (pre-existing, not introduced by Phase I).
- **Rollback**: revert `routes/web.php`, `app/Providers/AppServiceProvider.php`, `resources/views/layouts/app.blade.php`, `resources/views/partials/*.blade.php`, and `resources/views/pages/*.blade.php` to their pre-Phase-I versions; revert `tests/Feature/ExampleTest.php`; delete `app/Http/Controllers/PageController.php`, `app/Cms/Content.php`, `tests/Feature/PublicPagesTest.php`. No migration to roll back.

### 2026-06-15 — Production Hardening Sprint 1: SEO metadata, theme-settings caching, richtext sanitization

- **What changed**: Resolved the three Must-Fix findings from the Post-Phase-I Production Readiness Audit (B1 SEO metadata dead, P1 theme-settings N+1, S2 unsanitized richtext), plus the audit's double-`<head>` cleanup item (B2). No public design/markup/CSS changes — only `<head>` metadata, theme-settings loading, and richtext output were touched.
  - **SEO metadata wired up (B1)**: `resources/views/partials/head.blade.php` now computes `<title>`, `<meta name="description">`, `<meta name="keywords">`, `<meta name="robots">`, `<link rel="canonical">`, and `og:title`/`og:description`/`og:url`/`og:type`/`og:image` from the resolved `Page` model (`$page->meta_title`, `meta_description`, `meta_keywords`, `robots`, `canonical_url`, `og_title`, `og_description`, `og_image_id`/`ogImage`), via a new `App\Cms\Content::pageMeta(?Page $page, string $column, ?string $default = null)` helper (same null/empty → fallback pattern as `Content::field()`). Falls back to each page's existing hardcoded `@section('title'|'meta_description'|'meta_keywords', ...)` values (read via `$__env->yieldContent(...)`) when the corresponding `pages` column is empty, to `robots = 'index, follow'` when empty, to `url()->current()` for `canonical_url` when empty, and to the resolved title/description for `og_title`/`og_description` when empty. `og:image` is omitted entirely if `og_image_id` is unset. `PageController::show()` now eager-loads the `ogImage` relation alongside `sections`/`items`/`fields`/`media` so this adds zero extra queries per request.
  - **Theme settings load once per request (P1)**: `AppServiceProvider::boot()` now binds `loadThemeSettings()` behind a `cms.theme-settings` container singleton, resolved once by the `View::composer('*', ...)` callback (still re-registered on every view, but `app()->make('cms.theme-settings')` returns the same cached `Collection` after the first resolution). A `$this->app->terminating(...)` callback calls `forgetInstance('cms.theme-settings')` after each request/test completes, so the next request re-queries fresh data — live settings updates are preserved, no stale-cache window. Verified via query log: theme `settings` query count per request dropped from 8 to 1 (`tests/Feature/PublicPagesTest::test_theme_settings_are_loaded_once_per_request`).
  - **Richtext sanitization (S2)**: added `ezyang/htmlpurifier` (`^4.18`) as a new Composer dependency (the project's first new package since Phase B). `App\Cms\Content::richtext(?string $html): string` runs `content.body` (and any other `richtext`-typed field) through an `HTMLPurifier` instance (memoized per-request in a static property) configured with an allowlist: `p[class]`, `br`, `strong`/`em`/`b`/`i`/`u`, `h1`-`h6[class]`, `ul`/`ol`/`li[class]`, `a[href|title|target|rel]` (with `HTML.TargetBlank` so external links get `rel="noreferrer noopener"`), `blockquote`, `span[class]`. All 4 pages that render `content.body` via `{!! $contentBody !!}` (`about`, `services`, `solutions`, `contact` — `resources/views/pages/{about,services,solutions,contact}.blade.php`) now render `{!! Content::richtext($contentBody) !!}` instead. HTMLPurifier's compiled-definition cache is written to `storage/framework/cache/purifier/` (new directory, gitignored).
  - **Double `<head>` fixed (B2 / audit cleanup)**: `resources/views/partials/head.blade.php` no longer opens its own `<head>`/`</head>` — `resources/views/layouts/app.blade.php`'s `<head>@include('partials.head')</head>` is now the only `<head>` wrapper. Verified by `tests/Feature/PublicPagesTest::test_only_one_head_tag_is_rendered` (asserts exactly one `<head` and one `</head>` in the rendered page).
- **Files created**: `storage/framework/cache/purifier/.gitignore` (new HTMLPurifier definition-cache directory).
- **Files modified**: `composer.json`/`composer.lock` (added `ezyang/htmlpurifier`), `app/Providers/AppServiceProvider.php` (singleton-cached theme settings + `terminating` cleanup), `app/Http/Controllers/PageController.php` (eager-load `ogImage`), `app/Cms/Content.php` (new `pageMeta()` and `richtext()` helpers), `resources/views/partials/head.blade.php` (full SEO/OG metadata block, single `<head>`), `resources/views/pages/{about,services,solutions,contact}.blade.php` (`Content::richtext($contentBody)`), `tests/Feature/PublicPagesTest.php` (+6 tests).
- **Database changes**: none.
- **Routes**: none added/changed.
- **Packages**: added `ezyang/htmlpurifier` (`^4.18`) — see "RichText sanitization tradeoffs" in `notes.md`.
- **Why**: closes all three Must-Fix findings from the 2026-06-15 Post-Phase-I Production Readiness Audit so the public site can move from "Production Ready After Minor Fixes" to "Production Ready".
- **Verified**: `php artisan test` — 75 passed (276 assertions), up from 69 passed (248 assertions). New tests: `test_page_meta_fields_render_when_present`, `test_page_meta_fields_fall_back_to_defaults_when_absent`, `test_only_one_head_tag_is_rendered`, `test_theme_settings_are_loaded_once_per_request`, `test_richtext_content_is_sanitized_against_stored_xss`, `test_richtext_content_preserves_legitimate_formatting`. All 69 pre-existing tests continue to pass unmodified.
- **Remaining known gaps (not addressed by this sprint, see audit "Recommended"/"Nice to Have" items)**:
  - `Content::field()`/`itemField()` still don't guard against the `Media`-vs-`string` return-type ambiguity (M2 in the audit).
  - `Content::items()`'s per-section (not per-item) fallback granularity is unchanged (A1).
  - `home.blade.php`'s `Content::itemField($slide, 'heading')` call (line 73) still has no explicit `$default` (E1).
  - The three unsynchronized schema sources (`config/cms/templates.php`, `PagesSeeder::ITEM_COUNTS`, per-view default arrays) are unchanged (M1).
  - Dead admin-editable fields (`about.image`, `brand_logos` item `url`, `stats.heading`/`subheading` on non-home pages, `appointment.subheading`) are unchanged (E2).
  - Inline `style="...url('{{ $heroBg }}')..."` hero-background pattern is unchanged (S1, low risk).

### 2026-06-15 — Phase H: Contact Messages

- **What changed**: implemented a complete Contact Messages module — public form submission (contact page + homepage appointment form), persistence, and an admin inbox.
  - **Database**: new `contact_messages` table (`database/migrations/2026_06_15_100010_create_contact_messages_table.php`): `name`, `email`, `phone` (nullable), `subject` (nullable), `message` (text), `source_page` (string — `'contact'` or `'home'`), `ip_address` (nullable), `status` (string, default `'new'`), `read_at` (nullable timestamp), timestamps. Indexes on `status` and `created_at`. No soft deletes — `status = 'archived'` is the retention mechanism.
  - **Model**: `app/Models/ContactMessage.php` — `$fillable` covers all public-submittable + server-set fields except `read_at` (intentionally excluded from `$fillable`, see Security below). `read_at` cast to `datetime`. `scopeUnread()` (`status = 'new'`) and `markAsRead()` (transitions `new` → `read`, sets `read_at = now()`, no-op if already read/archived).
  - **Public submission**: new `App\Http\Controllers\ContactMessageController::store(Request $request, string $sourcePage)`. Two routes share this controller, following the existing `app(PageController::class)->show(...)` closure convention in `routes/web.php`:
    - `POST /contact` → `name('contact.submit')`, `$sourcePage = 'contact'`
    - `POST /appointment` → `name('appointment.submit')`, `$sourcePage = 'home'`
    Both forms (`resources/views/pages/contact.blade.php` "Send Us a Message" form, `resources/views/pages/home.blade.php` "Online Appointment" form) now have `method="POST"`, `action="{{ route(...) }}"`, `@csrf`, `name=` attributes on all fields, `old()` value retention, and inline success/`$errors` alert blocks (matching the admin `session('success')` alert pattern). The "Select Service"/"Choose A Service" dropdowns now post to the `subject` column.
  - **Admin module**: `app/Http/Controllers/Admin/ContactMessageController.php` — `index()` (paginated 20/page, newest-first, unread count badge), `show()` (full detail, auto-calls `markAsRead()`), `archive()` (sets `status = 'archived'`, PATCH). New routes `admin.contact-messages.{index,show,archive}` under the existing `auth` middleware group. New views `resources/views/admin/contact-messages/{index,show}.blade.php` follow the `admin.pages.index` table/badge/`@extends('admin.layouts.app')` conventions.
  - **Sidebar**: `resources/views/admin/partials/sidebar.blade.php` — "Contact Messages" link now points to `route('admin.contact-messages.index')` with `routeIs('admin.contact-messages.*')` active-state binding (was `href="#"`).
  - **Dashboard**: `/admin/dashboard` route is now `Admin\DashboardController::index()` (was a bare closure) supplying `$totalMessages` (`ContactMessage::count()`) and `$unreadMessages` (`ContactMessage::unread()->count()`). The "Messages" metric card in `admin/dashboard/index.blade.php` shows the real total, an "N unread" sub-line when `$unreadMessages > 0`, and links to the inbox. Pages/Services/Solutions cards remain hardcoded (out of scope).
  - **Incidental fix**: `routes/web.php` was missing `use Illuminate\Http\Request;` for the pre-existing `admin.password.email` closure (latent type-hint bug, never exercised by tests) — added while touching the route-import block for this phase's new closures.
- **Validation rules** (`ContactMessageController::store`): `name` required|string|max:255, `email` required|email|max:255, `phone` nullable|string|max:50, `subject` nullable|string|max:255, `message` required|string|max:5000. Failures redirect back with `$errors` (no JSON/API surface).
- **Security review**:
  - No mass-assignment risk: `source_page`, `ip_address`, and `status` are always set server-side in the controller, never from `$validated` request input.
  - `read_at` deliberately excluded from `$fillable` — `markAsRead()` sets it via direct attribute assignment + `save()`, so it can never be set via `update($request->all())`-style calls even if one is added later.
  - Admin views (`index.blade.php`, `show.blade.php`) render all user-submitted fields (`name`, `email`, `subject`, `message`, etc.) via `{{ }}` (auto-escaped) — no `{!! !!}` anywhere in the new views, so stored XSS via a contact form is not possible.
  - `ip_address` captured via `$request->ip()` (standard Laravel, not user-controllable beyond normal proxy/IP semantics).
  - CSRF: both public forms use the default `VerifyCsrfToken` middleware + `@csrf`.
- **Out of scope (explicitly deferred)**: email notifications, CAPTCHA/spam protection, file uploads, threaded replies, API endpoints — see "Future extension ideas" in `notes.md`.
- **Files created**: `database/migrations/2026_06_15_100010_create_contact_messages_table.php`, `app/Models/ContactMessage.php`, `app/Http/Controllers/ContactMessageController.php`, `app/Http/Controllers/Admin/ContactMessageController.php`, `app/Http/Controllers/Admin/DashboardController.php`, `resources/views/admin/contact-messages/{index,show}.blade.php`, `tests/Feature/ContactMessagesTest.php`, `tests/Feature/Admin/ContactMessagesTest.php`.
- **Files modified**: `routes/web.php` (new public + admin routes, dashboard route → controller, `Request` import fix), `resources/views/pages/contact.blade.php` (form wiring), `resources/views/pages/home.blade.php` (appointment form wiring), `resources/views/admin/partials/sidebar.blade.php` (link wired), `resources/views/admin/dashboard/index.blade.php` (real message metric).
- **Verified**: `php artisan test` — 86 passed (up from 75), no regressions. New tests cover: message creation via both public forms, validation failures (missing fields, invalid email), IP capture, admin index/show/archive, mark-as-read on view, dashboard counts, guest redirect.
- **Remaining roadmap after Phase H**:
  - Real forgot-password flow (token + email).
  - Pages/Services/Solutions dashboard counts still hardcoded.
  - Menu management admin CRUD (`menus`/`menu_items` schema exists, no UI).
  - Email notifications / spam protection for contact messages, if ever required (currently explicitly out of scope).
  - The Class B/C hardcoded-content items noted in prior Phase E.1/I/Production-Hardening entries remain unchanged.

### 2026-06-15 — Phase H.1: Contact Messages Hardening

- **What changed**: resolved the Must Fix finding and applied the high-value Recommended fixes from the Phase H production audit.
  - **Dashboard count test fixed**: `tests/Feature/Admin/ContactMessagesTest.php::test_dashboard_shows_real_message_counts` no longer uses `assertSee('2')` (a substring match that trivially passed because the hardcoded "Total Pages" card renders `12`). It now asserts `assertViewHas('totalMessages', 2)` and `assertViewHas('unreadMessages', 1)`, plus a new `test_dashboard_shows_zero_messages_when_none_exist` test asserting `0`/`0` and that "unread" isn't rendered. Both would now fail if `DashboardController` computed the counts incorrectly.
  - **Rate limiting added**: `routes/web.php` — `contact.submit` and `appointment.submit` now have `->middleware('throttle:10,1')` (10 requests/minute per IP), mirroring the existing `admin.login.submit` (`throttle:5,1`) convention. Covered by `test_contact_form_submission_is_rate_limited` and `test_appointment_form_submission_is_rate_limited` (10 successful submissions, 11th returns HTTP 429, DB count stays at 10).
  - **Empty `phone`/`subject` normalized to `null`**: `app/Http/Controllers/ContactMessageController.php::store()` now converts empty-string `phone`/`subject` (and absent keys) to `null` before `ContactMessage::create()`. This makes the admin views' `?? '—'` placeholder logic behave consistently regardless of whether the form field was submitted blank or omitted. Covered by `test_empty_phone_and_subject_are_stored_as_null` and `test_omitted_phone_and_subject_are_stored_as_null`.
  - **Archive workflow hardened**: `app/Http/Controllers/Admin/ContactMessageController.php::archive()` now calls `$contactMessage->markAsRead()` before setting `status = 'archived'`. This guarantees `archived` messages always have a non-null `read_at`, closing the audit's "archived + null read_at" edge case via the "always mark read first" approach (chosen over documenting it as a valid state, since enforcing the invariant is a one-line change and keeps `read_at` meaningful as "this message has been seen by an admin"). Covered by `test_archiving_an_unread_message_marks_it_as_read_first`.
  - **Additional regression coverage**: `test_guest_is_redirected_from_contact_message_show_and_archive` (guest redirect for `show`/`archive`, previously only `index` was covered), `test_viewing_an_already_read_message_does_not_change_read_at` (idempotency of `markAsRead()`), `test_admin_views_escape_message_content_against_stored_xss` (stored-XSS regression for both `index` and `show` admin views, mirroring `PublicPagesTest::test_richtext_content_is_sanitized_against_stored_xss`), `test_contact_form_submission_cannot_override_source_page_status_or_ip` (mass-assignment guard — posting `source_page`/`status`/`ip_address` in the request body does not affect the stored row).
- **Files modified**: `app/Http/Controllers/ContactMessageController.php` (normalize empty `phone`/`subject` to `null`), `app/Http/Controllers/Admin/ContactMessageController.php` (`archive()` calls `markAsRead()` first), `routes/web.php` (`throttle:10,1` on `contact.submit`/`appointment.submit`), `tests/Feature/ContactMessagesTest.php` (+5 tests), `tests/Feature/Admin/ContactMessagesTest.php` (+4 tests, 1 assertion rewrite).
- **Database changes**: none.
- **Verified**: `php artisan test` — 96 passed (369 assertions), up from 86. No regressions.
- **Audit items not changed** (assessed as Nice to Have, deferred): Tailwind pagination view inside the Bootstrap admin UI (pre-existing, also affects `admin/media/index.blade.php`); "Total Messages" includes archived rows (kept as a lifetime total — no requirement to split); per-request dashboard `COUNT` queries (negligible at current scale); duplicated status-badge `match` expression across `index`/`show` views; `status` remains a plain string column (no enum) — all noted in `notes.md` for future reference.
- **Rollback**: revert `app/Providers/AppServiceProvider.php`, `app/Http/Controllers/PageController.php`, `app/Cms/Content.php`, `resources/views/partials/head.blade.php`, `resources/views/pages/{about,services,solutions,contact}.blade.php`, `tests/Feature/PublicPagesTest.php`; run `composer remove ezyang/htmlpurifier`; delete `storage/framework/cache/purifier/`.
